{
  "name": "@dira/subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dira/markets",
    "deploy:hosted": "graph deploy --product hosted-service dira/markets",
    "create-local": "graph create --node http://localhost:8020/ dira/markets",
    "remove-local": "graph remove --node http://localhost:8020/ dira/markets",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dira/markets",
    "auth": "graph auth --product hosted-service"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.60.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
